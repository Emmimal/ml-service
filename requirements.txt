# ── Runtime dependencies ───────────────────────────────────────────────────────
# All versions are pinned to the minor level.
# Patch upgrades (e.g. 0.115.x) are generally safe and can be unpinned
# if your CI runs a full test suite on every dependency update.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
scikit-learn==1.5.1
joblib==1.4.2
numpy==1.26.4
